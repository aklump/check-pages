{
    "sleep__sleep": {
        "description": "A number of seconds to pause execution",
        "type": "integer",
        "pattern": "^\\d+$",
        "examples": [
            1,
            30
        ]
    }
}
