{
    "type": "object",
    "oneOf": [
            {
            "required": [
                "table",
                "set"
            ]
        },
        {
            "required": [
                "table",
                "contains"
            ]
        },
        {
            "required": [
                "table",
                "not contains"
            ]
        },
        {
            "required": [
                "table",
                "is"
            ]
        },
        {
            "required": [
                "table",
                "is not"
            ]
        },
        {
            "required": [
                "table",
                "matches"
            ]
        },
        {
            "required": [
                "table",
                "not matches"
            ]
        },
        {
            "required": [
                "table",
                "count"
            ]
        },
        {
            "required": [
                "table",
                "text"
            ]
        }
    ],
    "additionalProperties": false,
    "properties": {
        "table": {
            "$ref": "#/definitions/table__table"
        },
        "set": {
            "$ref": "#/definitions/value__set"
        },
        "contains": {
            "$ref": "#/definitions/contains"
        },
        "not contains": {
            "$ref": "#/definitions/contains"
        },
        "is": {
            "$ref": "#/definitions/equals"
        },
        "is not": {
            "$ref": "#/definitions/equals"
        },
        "matches": {
            "$ref": "#/definitions/matches"
        },
        "not matches": {
            "$ref": "#/definitions/matches"
        },
        "count": {
            "$ref": "#/definitions/count__count"
        },
        "text": {
            "$ref": "#/definitions/text"
        },
        "attribute": {
            "$ref": "#/definitions/attribute"
        }
    }
}
